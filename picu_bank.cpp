#include <iostream>

using namespace std;

int main(int argc, char const *argv[]) {
	int dollar, addition, month, addition2, target, testcase;
	//Dollar = D, Addition = A, Addition2 = B, months = M, Target = X

	std::cin >> testcase;
	while (testcase--)
	{
		std::cin >> dollar >> addition >> month >> addition2 >> target;
		/* algorithm for counting the months*/
	}
	std::cout << month << '\n';
	return 0;
}
